"use client";

import Button from "@/app/auth/(formbased)/components/client/Button";

const GoogleSignupButton = () => {
  return (
    <Button
      text="구글로 가입하기"
      type="button"
      backgroundColorTailwind="bg-sky-500"
      onClick={() => {}}
    />
  );
};

export default GoogleSignupButton;
