"use client";

import Button from "@/app/auth/(formbased)/components/client/Button";

const KakaoLoginButton = () => {
  return (
    <Button
      text="카카오로 시작하기"
      type="button"
      backgroundColorTailwind="bg-yellow-500"
      onClick={() => {}}
    />
  );
};

export default KakaoLoginButton;
