"use client";

import Button from "@/app/auth/(formbased)/components/client/Button";

const NaverLoginButton = () => {
  return (
    <Button
      text="네이버로 시작하기"
      type="button"
      backgroundColorTailwind="bg-green-500"
      onClick={() => {}}
    />
  );
};

export default NaverLoginButton;
